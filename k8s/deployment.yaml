apiVersion: apps/v1
kind: Deployment
metadata:
  name: campusx-app
  labels:
    app: campusx-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: campusx-app
  template:
    metadata:
      labels:
        app: campusx-app
    spec:
      containers:
        - name: campusx-container
          image: campusx-app:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 5000
          env:
            - name: NODE_ENV
              value: "production"
            - name: PORT
              value: "5000"
            - name: DATABASE_PATH
              value: "/app/uploads/database.sqlite"
          volumeMounts:
            - name: campusx-storage
              mountPath: /app/uploads
          readinessProbe:
            httpGet:
              path: /api/posts
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 5000
            initialDelaySeconds: 15
            periodSeconds: 20
      volumes:
        - name: campusx-storage
          persistentVolumeClaim:
            claimName: campusx-pvc
